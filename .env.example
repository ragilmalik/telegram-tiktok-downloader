# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Public URL of your server (without trailing slash)
# This is the URL where your server can be accessed from the internet
# Examples:
#   http://your-server-ip:3456
#   https://yourdomain.com
#   http://123.456.789.012:3456
# NOTE: This is optional now. Videos are sent directly to Telegram.
# Only used as fallback for files too large to send directly.
PUBLIC_URL=http://your-server-ip:3456

# Server Ports
# Web Server Port (default: 3456)
# Changed from 3000 to avoid conflicts with common dev servers
PORT=3456

# Admin Panel Port (default: 5789)
# Changed from 5000 to avoid conflicts with Flask and other apps
ADMIN_PORT=5789

# File Management
# Maximum age of downloaded files in hours before auto-deletion (default: 24)
# Note: Files are auto-deleted after sending to save space
MAX_FILE_AGE_HOURS=24

# Cleanup interval in minutes (default: 60)
CLEANUP_INTERVAL_MINUTES=60

# Performance & Queue Settings
# Maximum number of concurrent downloads (default: 3)
# Increase for better throughput, decrease if server is struggling
MAX_CONCURRENT_DOWNLOADS=3

# Rate Limiting
# Minimum time in minutes between requests per user (default: 1)
# Prevents abuse and API rate limiting
RATE_LIMIT_MINUTES=1

# Retry Settings
# Maximum number of retry attempts for failed downloads (default: 3)
# Uses exponential backoff (2s, 4s, 8s)
MAX_RETRIES=3

# Maximum number of consecutive Telegram polling errors before shutting down (default: 5)
# Prevents infinite error loops that flood your terminal
# The bot will exit gracefully after this many consecutive connection errors
# Counter resets after 5 minutes of successful connection
MAX_POLLING_ERRORS=5
